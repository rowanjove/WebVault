# 更新日志

## v0.1.1 - 2026-09-21

稳定性、安全与回放修复。

### 修复
- 回放不再出现 Unauthorized：主文档和静态资源都带上访问令牌，跨源 iframe 不再依赖 cookie
- 屏蔽应用界面与回放页的鼠标右键菜单
- 整站「全域名」范围不再把 `.com.cn` / `.co.uk` 等公共后缀整段爬进去
- 「仅当前页」按规范化 URL 匹配；「路径前缀」按同源判断，避免爬到站外
- 监控/仪表盘的「比对」「回放」会打开对应站点、页面和快照
- 取消任务会真正关掉浏览器；删除站点前先停该站抓取
- 崩溃后残留的 running 任务启动时标记为失败
- 任务列表里 cancelled 不再显示成失败

### 安全
- 登录地址只接受 http/https，Chromium 启动参数与网址用 `--` 隔开
- 登录 Cookie / localStorage 用 Windows DPAPI 加密后写入本地数据库
- 回放默认不再向活网拉取缺失资源
- 保存凭证接口不再把 Cookie 明文回传前端

### 归档与备份
- WACZ 导入按 WARC 真实偏移建立页面、资源和搜索索引
- 备份恢复先把现有数据挪到一边，失败可回滚
- 备份包含浏览器登录配置目录（跳过 Cache）
- 监控有浏览器时改走 CDP 抓取，变动快照带样式和图片

### 其它
- 中文搜索按字切分，标题与摘要显示正常
- 整站确认出现在创建站点之前，取消不会留下空站点
- RSS 同步提示改为「发现链接、尚未抓取」
- 登录态 localStorage 写入目标网站而不是空白页
